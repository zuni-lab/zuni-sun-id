{
  "name": "zuni-dapp-template",
  "version": "0.0.0",
  "repository": "git@github.com:zuni-lab/zuni-dapp-template.git",
  "devDependencies": {
    "husky": "^9.0.11"
  },
  "private": true,
  "scripts": {
    "prepare": "husky",
    "dev": "bun --filter='ui' dev",
    "build": "bun --filter='ui' run build",
    "start": "bun --filter='ui' start",
    "lint": "bun --filter='ui' run lint"
  },
  "workspaces": [
    "ui",
    "packages/*"
  ],
  "dependencies": { "@radix-ui/react-dialog": "^1.1.1", "zustand": "^4.5.5" }
}
