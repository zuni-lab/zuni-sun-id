{
  "name": "zuni-sun-id",
  "version": "0.0.1",
  "repository": "git@github.com:zuni-lab/zuni-sun-id.git",
  "private": true,
  "scripts": {
    "dev": "bun --filter='ui' dev",
    "build": "bun --filter='ui' run build",
    "start": "bun --filter='ui' start",
    "lint": "bun --filter='ui' run lint"
  },
  "workspaces": [
    "ui",
    "packages/*"
  ],
  "trustedDependencies": ["ui"]
}
